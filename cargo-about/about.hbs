# Third-Party Licences

This file lists every crate `epd-photoframe-server` links against, grouped
by licence, with the verbatim upstream licence text. Generated by
`cargo about generate -c cargo-about/about.toml -o THIRD_PARTY_LICENSES.md cargo-about/about.hbs`;
re-run after a dependency change.

`epd-photoframe-server` itself is licensed AGPL-3.0-only — see
[LICENSE](./LICENSE). Bundled non-code assets (fonts, blue-noise tile)
are documented in [NOTICE](./NOTICE).

## Overview

{{#each overview}}
- {{{name}}} — {{count}} crate(s)
{{/each}}

## Per-licence detail

{{#each licenses}}
### {{{name}}}

Used by:
{{#each used_by}}
- [`{{crate.name}}` v{{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```
{{{text}}}
```

{{/each}}
